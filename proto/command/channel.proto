syntax = "proto3";

package sercom.protocol.command;

import "proto/domain/channel.proto";

/*
 * Create a channel inside a hub.
 * Only OWNER / ADMIN.
 * Hub_id is required to know which hub to create the channel in.
 */
message CreateChannel {
  string hub_id = 1;
  string name = 2;
  sercom.protocol.domain.ChannelType type = 3;
}

message ChannelChange {
  oneof change {
    string name = 1;
  }
}

/*
 * Update channel settings.
 * Only OWNER / ADMIN.
 */
message UpdateChannel {
  string channel_id = 1;
  repeated ChannelChange changes = 2;
}

/*
 * Permanently delete a channel.
 * Only OWNER.
 */
message RemoveChannel {
  string channel_id = 1;
}
