syntax = "proto3";

package sercom.protocol.domain;

enum ChannelType {
    ChannelType_UNSPECIFIED = 0;
    ChannelType_TEXT = 1;
    ChannelType_VOICE = 2;
}

message Channel {
    uint64 id = 1;
    string name = 2;
    ChannelType type = 3;
}
