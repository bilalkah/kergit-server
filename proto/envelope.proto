syntax = "proto3";

package sercom.protocol;

message Envelope {
    uint32 version = 1;

    enum Type {
        UNKNOWN = 0;
        PING = 1;
        PONG = 2;
        SESSION_BOOTSTRAP = 3;
        FETCH_MESSAGES = 4;
        AUTH = 5;
    }

    Type type = 3;
    bytes payload = 4;
}
