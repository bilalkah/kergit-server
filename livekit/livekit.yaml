port: 7880
rtc:
  tcp_port: 7881
  udp_port: 7882
  # enable_loopback_candidate: true   # REQUIRED for localhost
  # use_external_ip: false          # implicit, just remove it
  node_ip: 192.168.0.15

keys:
  dev: a780bc5d7c0fddb53308e170d3bbef4e01fdf7d28a3c171cde429c58b794ddd4

room:
  enabled_codecs:
    - mime: audio/opus
    - mime: video/vp8
    - mime: video/h264

logging:
  level: debug
# docker compose -f docker/docker-compose.yml up -d livekit caddy
